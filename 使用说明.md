# 团队日报提交网站 — 使用说明

## 一、本地运行（开发/测试）

### 1. 安装依赖

进入 `daily-report-site` 目录，双击运行 `start.bat`，或手动执行：

```bash
cd C:\Users\Administrator\WorkBuddy\2026-05-07-task-1\daily-report-site
npm install
```

> 如果 `better-sqlite3` 安装失败（Windows 常见），请先安装构建工具：
> ```bash
> npm install --global windows-build-tools
> npm install
> ```

### 2. 启动服务

```bash
node server.js
```

启动后看到提示：`日报提交网站已启动：http://0.0.0.0:3000`

### 3. 访问网站

- 本机访问：`http://localhost:3000`
- 局域网其他电脑：`http://你的局域网IP:3000`
  - 查看本机 IP：`ipconfig`（找 IPv4 地址）

### 4. 首次使用

1. 打开网站后，点击「新成员？点击注册」
2. 填写姓名和密码，注册第一个账号
3. 登录后提交一条测试日报
4. 点击右上角「汇总查看」查看所有人提交情况

---

## 二、云服务器部署（外网访问）

### 方案一：Linux 服务器（推荐）

#### 步骤1：安装 Node.js

```bash
# Ubuntu / Debian
curl -fsSL https://deb.nodesource.com/setup_20.x | sudo -E bash -
sudo apt-get install -y nodejs

# 验证
node -v   # 应显示 v20.x.x
npm -v
```

#### 步骤2：上传项目文件

将整个 `daily-report-site` 文件夹上传到服务器，例如 `/var/www/daily-report`

```bash
cd /var/www/daily-report
npm install --production
```

#### 步骤3：使用 PM2 守护进程

```bash
# 安装 PM2
sudo npm install -g pm2

# 启动服务
pm2 start server.js --name daily-report

# 设置开机自启
pm2 startup
pm2 save
```

#### 步骤4：配置 Nginx 反向代理

```bash
sudo apt install nginx
```

编辑 `/etc/nginx/sites-available/daily-report`：

```
server {
    listen 80;
    server_name your-domain.com;  # 改为你的域名或服务器IP

    location / {
        proxy_pass http://localhost:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }
}
```

```bash
sudo ln -s /etc/nginx/sites-available/daily-report /etc/nginx/sites-enabled/
sudo nginx -t
sudo systemctl restart nginx
```

#### 步骤5：（可选）配置 HTTPS

```bash
sudo apt install certbot python3-certbot-nginx
sudo certbot --nginx -d your-domain.com
```

---

### 方案二：Windows 服务器

#### 步骤1：安装 Node.js

从 https://nodejs.org/ 下载安装 LTS 版本。

#### 步骤2：上传并安装依赖

将 `daily-report-site` 文件夹复制到服务器，例如 `C:\daily-report`

在文件夹中打开命令行，运行：

```cmd
npm install --production
```

#### 步骤3：注册为系统服务（nssm）

下载 nssm：https://nssm.cc/download

```cmd
nssm install DailyReport C:\Program Files\nodejs\node.exe
# 在弹窗中填写：
# Path: C:\Program Files\nodejs\node.exe
# Startup directory: C:\daily-report
# Arguments: server.js
```

然后启动服务：

```cmd
nssm start DailyReport
```

#### 步骤4：配置防火墙

在 Windows 防火墙中开放端口 `3000`（或配置 IIS 反向代理到 80/443 端口）。

---

## 三、功能说明

| 功能 | 说明 |
|------|------|
| 注册/登录 | 新成员自行注册，简单密码登录 |
| 提交日报 | 填写工作内容摘要，每日限提交一次 |
| 汇总查看 | 所有登录用户均可查看汇总表格 |
| 未完成标记 | 当日 18:00 后未提交显示为「未完成」（红色） |
| 成员管理 | 可禁用离职成员，使其不再出现在汇总中 |
| 历史查询 | 汇总页可选择任意日期查看历史日报 |

---

## 四、目录结构

```
daily-report-site/
├── server.js            # 后端服务入口
├── database.js          # 数据库初始化
├── package.json         # 依赖配置
├── start.bat            # Windows 一键启动脚本
├── daily-report.db      # SQLite 数据库（运行时自动生成）
└── public/
    ├── index.html       # 登录/注册页
    ├── dashboard.html   # 日报提交页
    ├── admin.html       # 汇总查看页
    ├── css/style.css   # 样式
    └── js/
        ├── login.js
        ├── dashboard.js
        └── admin.js
```

---

## 五、常见问题

**Q：端口 3000 被占用？**
修改 `server.js` 最后一行，或设置环境变量：
```bash
PORT=8080 node server.js
```

**Q：如何让外网访问？**
服务器部署后用 Nginx/IIS 反向代理，或直接开放端口（需在云服务器安全组中放行端口）。

**Q：数据库文件在哪里？**
`daily-report-site/daily-report.db`，可用 SQLite 工具直接打开查看或备份。
